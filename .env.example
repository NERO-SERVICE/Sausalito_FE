# Local dev 기본값: Vite proxy -> Django runserver
VITE_BACKEND_ORIGIN=http://127.0.0.1:8000

# 기본값은 /api/v1 (배포 시 Netlify function/reverse proxy 경유).
# 절대 URL은 허용하지 않으며, 필요 시 경로만 오버라이드하세요.
# VITE_API_BASE_PATH=/api/v1

# Netlify Functions 환경변수(사이트 설정 > Environment variables)
# BACKEND_ORIGIN=https://api.yourdomain.com
